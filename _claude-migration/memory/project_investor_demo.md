---
name: project_investor_demo
description: "The Albedo investor playground — a React/Vite interactive simulation of Albedo's compiler/runtime, separate repo at A:\\albedo-investor-demo"
metadata: 
  node_type: memory
  type: project
  originSessionId: d71250c2-12de-4514-b84f-c0cb715ed115
---

**Albedo Investor Playground** — an interactive, game-like *simulation* of Albedo's machinery for an investor pitch (funding imminent). NOT a slide deck. The thesis: investors should *feel* the causality — "I add a hook → tier changes → byte count changes → latency changes → here's what that costs my business." Shows what Albedo *will become* when the [[project_endgame]] 4-gate plan lands, not its current state.

**Location:** `A:\albedo-investor-demo` (standalone, deliberately OUTSIDE the main Albedo repo to keep it clean). React 19 + Vite + TypeScript + Tailwind v4 (`@tailwindcss/vite`) + Framer Motion + Monaco (`@monaco-editor/react`, loads from CDN) + Zustand + D3 (installed, but lattice uses fixed-position SVG not force-sim). Run: `npm --prefix A:\albedo-investor-demo run dev` (port 5173). Preview launch.json lives at `A:\AlBDO-v-0.1.0\.claude\launch.json` (untracked helper; name `albedo-demo`).

**Plan file:** `C:\Users\bisha\.claude\plans\claude-we-need-a-glittery-ripple.md` — 5 modules, build order, verification.

**The 5 modules** (user approved this scope):
1. **Compiler Playground** — ✅ DONE. Write/pick TSX → effect lattice lights up → tier badge (A/B/C) → live byte/opcode/latency readout. The star.
2. **Request Race** — ✅ DONE (2026-06-10). Canvas race, Albedo vs Next.js lanes, latency-faithful dots, live p50/p99 + throughput + histograms.
3. **Tier Budget Builder** — ✅ DONE (2026-06-10). Click-to-add palette → page canvas of wireframe blocks → live budget vs Next.js. Shared-runtime byte model.
4. **Business Impact / ROI case study** — ✅ DONE (2026-06-10). Archetype presets + sliders → server + runtime + **engineering headcount** + revenue-uplift savings. User explicitly wanted employee-cost (fewer devs needed) as a first-class lever.
5. **Port Diff** — ✅ DONE (2026-06-10). Next.js → Albedo side-by-side, 3 examples, tinted lines + unchanged-% + callouts. **ALL 5 MODULES DONE.**

**Module 5 files** (`src/modules/portdiff/`): `examples.ts` (3 curated examples — Interactivity [`'use client'` deleted, 92% unchanged — the punchline], Metadata+Data [getServerSideProps+next/head → `export const meta` + inline async, 74%], Server Data [Remix loader+useLoaderData → inline await, 70%]; per-line `nextTint`/`albedoTint` maps 1-based → cut/change/add; authored `unchangedPct`), `highlight.tsx` (regex TSX highlighter `highlightLine` → colored spans), `index.tsx` (example tabs + thesis "your devs write the same React", unchanged-% banner + legend, two `CodePanel`s w/ line-number gutter + tint bg + inset border, 3 callout cards). Tints: cut=orange #f97316, change/simplified=sky #38bdf8, add=teal #06d6a0, identical=untinted. Verified all 3 examples (tint counts match maps), clean console, **`npm run build` green** (459 modules).

**MAJOR REDESIGN — DONE 2026-06-10 (4 user requests):**
1. **Silver/black/white palette** (Apple/Gentle Monster) — rewrote `index.css` `@theme` tokens (bg #08080a, surface #111114, tier ramp A #ededf2 / B #a6a6b2 / C #8e8e9c, accent=white #f5f5f7, danger/Next.js/old=bronze #a18a66). Added `.glass` (liquid-glass) + `.silver-text` (gradient wordmark) utilities. Swept ALL hardcoded hex across components (TIER_META, race/stats ALBEDO=white/NEXTJS=bronze, business SEG 4-step silver ramp, portdiff TINT cut=bronze/change=silver/add=white, highlighter monochrome+bronze, Monaco theme). Ratio highlights (N×) changed from violet to white.
2. **Parallax hero** (`components/Hero.tsx`) — own scroll container w/ framer `useScroll`+`useTransform` (title fades/scales/rises, grid+orbs parallax, mouse-parallax on desktop) + scroll-reveal section (opaque `bg-bg z-10` so it covers the sticky stage) w/ 5 module cards + "Explore Albedo" CTA → `setView('compiler')`.
3. **Floating liquid-glass navbar** (`components/Navbar.tsx`) — desktop centered glass pill w/ brand + 5 links + framer `layoutId` sliding active pill; mobile glass bar + animated hamburger→X opening a glass sheet w/ staggered bouncy items. Spring animations throughout.
4. **Mobile responsive** — every module `flex-col md:flex-row` (or `grid-cols-1 md:grid-cols-N`), fixed-width panes → `w-full md:w-[X]`, controls `flex-wrap`, canvas/code panels stack w/ mobile min-heights, long headers `hidden lg:block`. No horizontal overflow at 375px (verified all 5).

**Structure change:** store `activeModule`→`view: 'hero'|ModuleId`; App = `Navbar` + AnimatePresence(`Hero` | `Workspace` w/ `pt-[62px] md:pt-[70px]` under floating navbar). **Deleted TopBar.tsx, Sidebar.tsx, Placeholder.tsx.** `MODULE_VIEWS` map in App.tsx.

**iOS-26 liquid-glass pass — DONE 2026-06-10 (user follow-up "more blur + rounded like iOS 26"):** beefed `.glass` (blur 30px, specular top edge + inner shadows), added `.glass-bar` (frosted module control/header strips — applied to race/business/portdiff top bars + budget browser-frame), `.glass-card` (frosted hero module cards), `.glass-panel`. **Workspace now floats as a big rounded panel** (App.tsx: `px-2/3 pb-2/3` margins + `glass-panel rounded-[22px] md:rounded-[28px] overflow-hidden`) over the black bg with the navbar overlapping its top — the key iOS look. Bumped radii across modules (rounded-md→rounded-xl, rounded-lg→rounded-2xl, cards rounded-[26px], nav sheet rounded-[26px]). **Hero refactored off `sticky`** → two clean sequential sections (hero h-screen + reveal bg-bg min-h-screen, verified sequential: heroBottom==revealTop==820, zero overlap) — the prior sticky+parallax caused a mid-scroll bleed-through bug now fixed; parallax transforms (title fade/scale/rise, grid/orbs, mouse-parallax) retained.

**STATUS: feature-complete + redesigned + iOS-26 glass. `npm run build` green (458 modules, ~412KB/129KB gz). Verified desktop (1366) + mobile (375). Next step = DEPLOY (Vercel `vercel --prod`).** Remaining optional polish: self-host Monaco (still CDN — offline breaks the editor; also why preview_screenshot times out on the compiler route).

**Module 4 files** (`src/modules/business/`): `model.ts` (`computeImpact`: 4 levers — **server** [Node needs redundant fleet ×3 regions, Albedo consolidates to a global HA pair → big instance reductions like 72→2; the asymmetry (Albedo NOT ×regions) is the fix that stopped server-saving reading $0 at low traffic], **runtime/bandwidth** [~30KB less JS/req × traffic × $0.09/GB egress], **engineering** [teamSize × devAutomation × salary — the emphasized lever], **revenue** [~340ms less TTI from shipping ~90% less JS × Amazon 1%/100ms, capped 8%]; 4 ARCHETYPES E-commerce/Media/SaaS/Enterprise; `fmtMoney`/`fmtViews`), `Slider.tsx` (range input w/ optional log scale for wide ranges), `index.tsx` (archetype top-bar, input column w/ stack select + 4 sliders + tunable devAutomation%, results: animated $total + 3yr, stacked breakdown bar, 4 ImpactCards each w/ expandable "how we calculated this" formula, secondary metric strip). Honest finding baked in: infra is the SMALLEST lever; engineering + revenue dominate (E-comm default $3.11M/yr = $651K eng + $2.45M rev + $7K server + $4K runtime). Verified: all 4 archetypes non-zero & sensible, sliders + expanders live.

**Module 3 files** (`src/modules/budget/`): `catalog.ts` (14 page sections across tiers w/ htmlBytes+islandBytes; **`computeBudget`** = the honest shared-runtime model: clientJs = runtimeBytes(once: hydrate 3100 if any C, else patch 283 if any B, else 0) + Σ islandBytes; nextJs = 85000 + count×1500; 4 PAGE_PRESETS Landing/Blog/Store/App), `Wireframe.tsx` (per-glyph div-based mini-representation, tier-tinted), `BudgetPanel.tsx` (client-JS headline, shell/islands cells, tier-breakdown stacked bar, Albedo-vs-Next.js compare bars + N× ratio; uses `AnimatedNumber`), `index.tsx` (palette grouped by tier w/ click-to-add; browser-framed page canvas; blocks support native HTML5 drag-reorder via `moveOver` + remove ×; empty state). Verified: Store preset → 4.3 KB client (3.1 shared + 1.2 islands), 3 islands, 3×A/1×B/2×C, vs Next.js 94 KB = 22× less JS. The "first island pays for the runtime, rest nearly free" story is the payoff.

**Module 2 files** (`src/modules/race/`): `stats.ts` (log-normal `sampleLatency`: Albedo median 0.07ms σ0.42, Next.js 3.5ms σ0.5; Box-Muller; `percentile`/`mean`; `fmtMs`/`fmtRps`/`fmtCount`), `useRaceEngine.ts` (single rAF owns sim-step + canvas draw; particles in refs, stats→React state throttled 12Hz; BASE_SCALE=430 so Next p50≈1.5s travel / Albedo≈30ms streaks; lanes pre-seed 140 samples so histograms read at rest, served stays 0 until start; throughput = concurrency/mean-latency, real extrapolation), `Histogram.tsx` (pure-SVG 24-bin + p50/p99 markers), `index.tsx` (controls: Send/Pause·Reset·concurrency 20/80/200·slow-mo; live scoreboard with N× ratio; canvas track w/ DOM lane labels+server endpoints; two distribution stat cards). Canvas motion-blur trails (long for fast Albedo, nil for slow Next.js). Verified: served & throughput ratios both ≈50× and consistent once distributions fill (early-run shows higher served ratio, converges). Sidebar's "Current Component"/"Active Effects" now gated to `activeModule==='compiler'` only.

**Engine (the heart):** `src/engine/{types.ts,simulate.ts,presets.ts}` — pure-TS approximation of Albedo's real rules (NOT real Albedo). `analyze(code)`: stripNoise → detectEffects (regex on useState/useEffect/useRef/useMemo/onClick·onChange/broadcast·useSharedSlot/async) → classifyTier (state/lifecycle hooks→C, onClick/broadcast→B, else→A) → estimateBytes (A=0 client, B=283 B patch decoder, C=3100+40/hook) → emitOpcodes → estimateLatency (A=no actions/0, B&C=8µs action; all serve p50 0.07ms). `fmtBytes`/`fmtLatency` helpers. 6 presets span A→C: Static(A), Button(B), Counter(C), Form(C), Dashboard(B), Chat(C).

**Shared state:** `src/store.ts` Zustand — `activeModule`, `code`/`activePresetId`/`analysis`, `setCode` (160ms-debounced in EditorPane), `loadPreset`. `src/modules.tsx` = nav metadata (compiler `live`, rest `soon`).

**Module 1 files:** `src/App.tsx` (shell, `min-w-[1120px]` + outer overflow-x-auto so it scrolls not crushes below floor; targets 1280px+), `components/{TopBar,Sidebar,AnimatedNumber}.tsx`, `modules/compiler/{index,EditorPane,LatticePane,OutputPane}.tsx`, `modules/Placeholder.tsx`. Pane widths: sidebar 188 / editor 330 / lattice flex / output 224. Lattice = 7 fixed nodes (pure/onClick/broadcast/async/useState/useEffect/useRef) + 7 edges, framer-motion glow on active. `PaneHeader` is exported from LatticePane and reused. Design tokens in `src/index.css` `@theme`: bg #080b14, surface #0f1422, tier-A #e2e8f0 / tier-B #38bdf8 / tier-C #a78bfa, accent #06d6a0, danger(Next.js) #f97316. Geist + Geist Mono from Google Fonts.

**Verified working (2026-06-10):** typecheck clean; all 3 tiers classify correctly via live preview eval (A=0 B client/no action, B=283 B/8µs, C=3.1KB+/8µs); presets + free-typing both reactive; lattice + badge + counters animate. Screenshot at 1366px = clean 4-pane layout.

**Known/deferred:** Monaco loads from jsdelivr CDN (fine online, breaks offline presentations + makes preview_screenshot time out on resize via network-idle — self-host later if needed). Modules 2–5 not built. Not deployed yet (target: Vercel, `vercel --prod`). Honest-voice disclaimer kept: action µs numbers are in-process (Gate 3 will publish end-to-end HTTP). Stakes: this is the funding pitch — see [[user_profile]], [[project_endgame]].
